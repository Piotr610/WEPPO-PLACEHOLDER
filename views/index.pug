extends layout

mixin product(imgSource, title, description, price, id)
    .col-sm-6.col-md-4
        .thumbnail.text-center
            img.img_responsive(src=imgSource alt="err")
            .caption
                h3=title
                p.description=description
                div.clearfix
                    .price.float-left=price + " $"
                    a.btn.btn-success.float-right(href="#" role="button") Add to cart
                    if session.admin
                        a.btn.btn-danger.float-right(href="/remove/"+id role="button") Remove
                        a.btn.btn-info.float-right(href="/edit/"+id role="button") Edit

block content
    .row
        each product in products
            +product(product.image, product.title, product.description, product.price, product.id)
